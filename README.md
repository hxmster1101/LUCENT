LUCENT OLD SYSTEM

This is the restored original index.js version before the recent automatic-store/gacha changes.

Railway variables:
DISCORD_TOKEN=your bot token
GUILD_ID=your server ID

Start command: npm start

Important: replace the GitHub index.js with this version and redeploy Railway. Do not mix it with newer index.js files.
